[tool.poetry]
name = "sarcasm-detector"
version = "0.1.0"
description = "A sarcasm detection application using Streamlit, TensorFlow, and Keras."
authors = ["Your Name <youremail@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
streamlit = "^1.0.0"
tensorflow = "^2.6.0"
keras = "^2.6.0"
scikit-learn = "^0.24.2"
pandas = "^1.3.3"
numpy = "^1.21.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"